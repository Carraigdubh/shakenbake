# Task 02-01-task-002: App CRUD functions

- **Status:** done
- **File:** `apps/cloud/convex/apps.ts`
- **Verified:** `npx tsc --noEmit` passed (exit 0)

## Functions

- `createApp` (mutation): Creates app with name, platform, orgId. Auth required.
- `listApps` (query): Lists apps for an org via `by_orgId` index.
- `getApp` (query): Direct get by app ID.
- `deleteApp` (mutation): Cascade-deletes all API keys via `by_appId` index, then deletes the app. Auth required.
