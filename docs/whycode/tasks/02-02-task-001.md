# Task 02-02-task-001: Convex HTTP router for ingestion

- **Plan:** 02-02 (Report Ingestion Endpoint)
- **Status:** done
- **Verified:** 2026-02-26T11:45:00Z

## Summary

Created `apps/cloud/convex/http.ts` with:
- POST /api/ingest HTTP action with Bearer token auth via validateApiKey internal query
- Request body validation for title, description, severity, category
- CORS headers (Access-Control-Allow-Origin: *, Methods, Headers) on all responses
- OPTIONS /api/ingest handler for CORS preflight
- Created `_generated/api.ts` stub for internal function references (cannot run codegen in cloud-live mode)

## Files

- `apps/cloud/convex/http.ts` (created)
- `apps/cloud/convex/_generated/api.ts` (created)
